import 'package:flutter/material.dart';

class AppColors {

  // Paleta principal (Eco-friendly)
  static const Color primary = Color(0xff17442d); // Verde oscuro
  static const Color secondary = Color(0xff9cc49e); // Verde claro

  static const Color Lightsecondary = Color(0xFF8BC34A); // Verde claro
  static const Color accent = Color(0xFFFFC107); // Amarillo (para llamadas a acción)

  // Neutrales
  static const Color background = Color(0xFFf1f0eA);
  static const Color card = Color(0xFFFFFFFF);
  static const Color textDark = Color(0xFF212121);
  static const Color textLight = Color(0xFF757575);
}